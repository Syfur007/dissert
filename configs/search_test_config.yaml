# Minimal Search Config for Sanity Check

search:
  method: "grid"
  output_dir: "outputs/searches/search_test"

grid:
  training:
    lr: [0.001]
    optimizer: ["adamw"]
  model:
    name: ["unet", "attention_unet"]
