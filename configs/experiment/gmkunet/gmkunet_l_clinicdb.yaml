# GMK-UNet (large) on ClinicDB — composes the shared base config, the
# ClinicDB dataset identity, and the large model-size fragment; only
# per-experiment overrides (batch size, run name) are listed here. See
# ../../base.yaml for every inherited default (early stopping, EMA,
# augmentation, checkpointing).
compose: ["../../base.yaml", "../../dataset/clinicdb.yaml", "../../model/gmkunet/l.yaml"]

dataset:
  batch_size: 16

logging:
  experiment_name: "gmkunet_l_clinicdb"
